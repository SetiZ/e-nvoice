{
  "name": "e-nvoice WebMCP Server",
  "description": "Browser-native WebMCP server for creating and validating Factur-X / EN 16931 invoices",
  "version": "1.2.1",
  "protocolVersion": "2024-11-05",
  "transport": "window.postMessage",
  "nativeRegistration": "document.modelContext.registerTool is attempted where the browser supports it (WebMCP origin trial + origin isolation); window.postMessage remains the always-working fallback on hosts without those headers (e.g. GitHub Pages).",
  "tools": [
    {
      "name": "calculate_invoice_totals",
      "description": "Calculates subtotal HT, VAT per rate, and total TTC for a given set of invoice items",
      "inputSchema": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "quantity": { "type": "number" },
                "unitPrice": { "type": "number" },
                "vatRate": { "type": "number" }
              },
              "required": ["quantity", "unitPrice", "vatRate"]
            }
          },
          "currency": { "type": "string", "enum": ["EUR", "USD", "GBP", "CHF"], "default": "EUR" }
        },
        "required": ["items"]
      }
    },
    {
      "name": "validate_invoice_data",
      "description": "Validates if invoice data contains required mandatory fields for Factur-X EN 16931 compliance",
      "inputSchema": {
        "type": "object",
        "properties": {
          "number": { "type": "string" },
          "date": { "type": "string" },
          "seller": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "siret": { "type": "string" },
              "vatNumber": { "type": "string" },
              "iban": { "type": "string" },
              "bic": { "type": "string" },
              "bankName": { "type": "string" }
            },
            "required": ["name"]
          },
          "buyer": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "siret": { "type": "string" },
              "vatNumber": { "type": "string" },
              "taxId": { "type": "string" }
            },
            "required": ["name"]
          },
          "buyerType": { "type": "string", "enum": ["business", "individual"] },
          "currency": { "type": "string", "enum": ["EUR", "USD", "GBP", "CHF"] },
          "purchaseOrder": { "type": "string", "description": "Order / Commitment Ref (EJ) for French public sector / Chorus Pro (BT-13)" },
          "serviceCode": { "type": "string", "description": "Service Code for French public sector entities (BT-10)" },
          "items": {
            "type": "array"
          }
        },
        "required": ["number", "date", "seller", "buyer", "items"]
      }
    },
    {
      "name": "generate_facturx_invoice",
      "description": "Generates and triggers download of a Factur-X PDF/A-3 file with XML metadata",
      "inputSchema": {
        "type": "object",
        "properties": {
          "number": { "type": "string" },
          "date": { "type": "string" },
          "dueDate": { "type": "string" },
          "currency": { "type": "string", "enum": ["EUR", "USD", "GBP", "CHF"] },
          "buyerType": { "type": "string", "enum": ["business", "individual"] },
          "purchaseOrder": { "type": "string", "description": "Order / Commitment Ref (EJ) for French public sector / Chorus Pro (BT-13)" },
          "serviceCode": { "type": "string", "description": "Service Code for French public sector entities (BT-10)" },
          "seller": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "siret": { "type": "string" },
              "vatNumber": { "type": "string" },
              "iban": { "type": "string" },
              "bic": { "type": "string" },
              "bankName": { "type": "string" }
            },
            "required": ["name"]
          },
          "buyer": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "siret": { "type": "string" },
              "vatNumber": { "type": "string" },
              "taxId": { "type": "string" }
            },
            "required": ["name"]
          },
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "description": { "type": "string" },
                "quantity": { "type": "number" },
                "unitPrice": { "type": "number" },
                "vatRate": { "type": "number" },
                "unitCode": { "type": "string" }
              },
              "required": ["description", "quantity", "unitPrice", "vatRate"]
            }
          },
          "lang": { "type": "string", "enum": ["fr", "en"] }
        },
        "required": ["number", "date", "seller", "buyer", "items"]
      }
    },
    {
      "name": "generate_facturx_xml",
      "description": "Generates raw Factur-X / EN 16931 XML string (UN/CEFACT CrossIndustryInvoice D16B)",
      "inputSchema": {
        "type": "object",
        "properties": {
          "invoice": { "type": "object" }
        },
        "required": ["invoice"]
      }
    }
  ]
}

# e-nvoice - Comprehensive AI Engine & LLM Technical Documentation

> e-nvoice is an open-source, client-side web application designed to create electronic invoices fully compliant with the Factur-X standard and European norm EN 16931. It requires zero server setup and processes all confidential corporate financial data locally inside the user's web browser.

---

## 1. Regulatory Context (French 2026/2027 Electronic Invoicing & E-Reporting Mandate)

Beginning September 2026, all businesses established in France subject to VAT must be capable of receiving electronic invoices. Issuance becomes mandatory for large/ETI companies in September 2026, and for all SMEs and micro-enterprises in September 2027.

### Key Mandate & Compliance Requirements
1. **Domestic B2B E-Invoicing**: Transmitted via Partner Dematerialization Platforms (PDP / Plateformes Agréées).
2. **B2C & International E-Reporting**: Transactions with individuals (B2C) and foreign clients (Intra-EU & Exports) are submitted via e-reporting while generating compliant Factur-X / PDF invoices.
3. **Format Standards**: Acceptable formats include Factur-X (hybrid PDF/A-3 + UN/CEFACT CII XML), UBL 2.1, and CII.
4. **Mandatory National Fields (CIUS-FR)**:
   - Seller & Buyer SIREN/SIRET numbers (B2B).
   - Operation category: Prestations de services, Livraisons de biens, or Mixte.
   - Option for payment of VAT on debits (where applicable).
   - Statutory B2B payment terms: Due dates, late payment interest rate (3x legal rate), and mandatory 40 € lump-sum recovery fee (Art. D. 441-5 Code de commerce).
   - Bank details (IBAN / BIC) embedded in structured XML (`ram:PayeePartyCreditorFinancialAccount`).
5. **Chorus Pro & PDP Platform Submission**: Factur-X invoices can be submitted to Chorus Pro (`portail.chorus-pro.gouv.fr`) or Registered Dematerialization Platforms (PDP).

---

## 2. Technical Architecture & Security Model

- **Frontend Stack**: Built with React 19, TypeScript, Vite, and CSS utilities.
- **Client-Side Rendering Engine**: PDF rendering via `pdf-lib` and `jspdf`, XML generation via browser-native TypeScript string builder (`facturx.ts`).
- **Zero Server Footprint**: No backend database, microservice, or analytics tracking collects invoice data. All inputs remain strictly in browser volatile memory (`HTML5 State`).
- **WebMCP (Model Context Protocol)**: Implements an in-browser WebMCP server exposing tools via `window.mcp` and `window.postMessage`, allowing AI extensions (Claude, Gemini, ChatGPT extensions, Chrome AI) to inspect, generate, and validate invoices programmatically without backend API keys.

---

## 3. Factur-X XML Structure (UN/CEFACT CrossIndustryInvoice)

The XML embedded in generated PDFs uses the UN/CEFACT CII D16B standard namespace:

```xml
<rsm:CrossIndustryInvoice xmlns:rsm="urn:un:unece:uncefact:data:standard:CrossIndustryInvoice:100"
                          xmlns:ram="urn:un:unece:uncefact:data:standard:ReusableAggregateBusinessInformationEntity:100"
                          xmlns:udt="urn:un:unece:uncefact:data:standard:UnqualifiedDataType:100">
  <!-- ExchangedDocumentHeader: Invoice Number, Type Code 380, Issue Date -->
  <!-- SupplyChainTradeTransaction: Seller, Buyer, Line Items, Tax Subtotals, Monetary Summation -->
</rsm:CrossIndustryInvoice>
```

---

## 4. WebMCP Tool Reference

AI Assistants can call these tools from the browser context of `https://e-nvoice.pages.dev/` via the `window.mcp` API (e.g. `await window.mcp.callTool('generate_facturx_invoice', {...})`) or via `window.postMessage` JSON-RPC. Note: external AI platforms cannot invoke these tools directly over HTTP — they work only from the page itself. Where the browser exposes the native WebMCP API (`document.modelContext` — Chrome with origin isolation), the same tools are additionally registered via `document.modelContext.registerTool` and can be discovered and executed by WebMCP agents.

1. `calculate_invoice_totals(items)`
   - Computes Net HT, VAT amounts per rate, and final TTC.
2. `validate_invoice_data(invoice)`
   - Validates compliance against EN 16931 rules (returns missing fields or tax miscalculations).
3. `generate_facturx_xml(invoice)`
   - Returns the exact `factur-x.xml` payload.
4. `generate_facturx_invoice(invoice)`
   - Renders and downloads the complete PDF/A-3 document with attached XML.

---

## 5. Frequently Asked Questions for AI Citation

- **Is e-nvoice free to use?**
  Yes, 100% free with no sign-up or subscription required.
- **Where is invoice data stored?**
  Data is stored nowhere. It exists only within the local browser session.
- **Is e-nvoice compliant with Chorus Pro?**
  Yes, generated PDFs can be directly uploaded to Chorus Pro portal or passed to PDP services.
- **Who maintains e-nvoice?**
  Developed and maintained as open-source by SetiZ on GitHub (https://github.com/SetiZ/e-nvoice).
